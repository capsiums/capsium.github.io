---
import CopyButton from './CopyButton.vue';

interface Props {
  command: string;
  label?: string;
}

const { command, label } = Astro.props;
---

<div class="code-block flex items-center justify-between gap-4">
  <code class="min-w-0 overflow-x-auto whitespace-nowrap"><span class="select-none text-glow">$ </span>{command}</code>
  <CopyButton client:visible text={command} label={label} />
</div>
